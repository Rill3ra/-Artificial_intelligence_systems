# -Artificial_intelligence_systems
Work completed at the university on artificial intelligence


# 🎮 Прогнозирование Объёмов Продаж Компьютерных Игр в Японии  
*Machine Learning проект с использованием LightGBM*

## 📌 Описание проекта

Цель проекта — **предсказать объемы продаж компьютерных игр в Японии (Japan_Sales)** на основе данных о продажах в других регионах, характеристиках игры, рейтингах и метаданных.  

Мы стремимся достичь **MAE ≤ 0.06**, что является довольно строгим требуемым порогом точности.

**Датасет:** `Video_Games.csv`  
**Количество записей:** 11 703 игр  
**Количество признаков после обработки:** 191

---

## 🔍 1. Исследование данных (EDA)

### 📊 1.1 Обзор признаков
Датасет содержит:

**Числовые признаки:**
- `NA_Sales`, `EU_Sales`, `Other_Sales`  
- `Critic_Score`, `Critic_Count`  
- `User_Score`, `User_Count`  
- `Total_Sales`  
- `Age_of_Game`  
- `Is_Adult_Rating`  
- Бинарные признаки платформ (`Is_PS4`, `Is_PC`, …)

**Категориальные признаки:**
- `Genre` (12 уникальных значений)
- `Publisher` (580)
- `Developer` (1696)
- `Rating` (8)

### ❗ 1.2 Пропущенные значения
| Признак     | NaN |
|-------------|-----|
| `Genre`     | 2   |
| `Publisher` | 54  |
| `Developer` | 6623|
| `Rating`    | 6769|

Особенно много пропусков у **Developer** и **Rating**, что влияет на стратегию предобработки.

### 📈 1.3 Что было дополнительно проанализировано:
- Распределение `Japan_Sales`
- Выбросы в продажах
- Корреляции числовых признаков
- Зависимость продаж от `Genre`, `Rating`, топовых издателей и разработчиков

---

## 🛠 2. Предобработка данных и Инженерия признаков

### 🔧 2.1 Числовые признаки
- **`User_Score`**: заменены значения `'tbd'` → `NaN`, затем конвертация в `float`.
- **`Total_Sales`**: вычислено как сумма продаж всех регионов (кроме Японии).
- **`Age_of_Game`**:  
  `2024 - Year_of_Release`, минимальное значение = 0.
- **`Year_of_Release`** и **`Name`** удалены (низкая полезность + риск утечки информации).

---

### 🧩 2.2 Признаки рейтинга
Создан бинарный признак:

```

Is_Adult_Rating = 1, если Rating ∈ {M, AO}
Иначе = 0

````

---

### 🎮 2.3 Обработка Platform
- Каждое значение платформы было очищено от спецсимволов.
- Для каждой платформы создан бинарный признак `Is_<Platform>`.
- Исходный столбец `Platform` удалён.

Этот подход помогает LightGBM эффективно использовать платформенную специфику.

---

### 🧱 2.4 Работа с категориальными признаками
- **`Genre`**, **`Rating`**, **`Publisher`**, **`Developer`**
- Для простых категориальных признаков → One-Hot Encoding.
- Для признаков с высокой кардинальностью (Publisher, Developer) применены:
  - Упрощение редких категорий.
  - Объединение малочастотных значений в категорию `"Other"`.

---

## 🤖 3. Модель и Пайплайн

### Используемый алгоритм:
**LightGBM Regressor**

Почему LightGBM:
- Отлично работает на табличных данных.
- Устойчив к шуму и пропускам.
- Поддерживает большие наборы признаков.
- Быстро обучается.

### Пайплайн
```python
pipeline = Pipeline(steps=[
    ('preprocessor', preprocessor),
    ('regressor', model)
])

pipeline.fit(X, y)
````

Вывод LightGBM:

```
[LightGBM] Number of data points in the train set: 11703, used features: 191
[LightGBM] Start training from score 0.0575
```

---

## 📉 4. Метрика качества

Основная метрика — **MAE (Mean Absolute Error)**
Цель: **≤ 0.06**

---

## 📦 5. Результаты

(здесь вы вставите финальный результат после обучения)

Пример формата:

```
MAE на валидации: 0.0542
Цель выполнена.
```

---

## 🚀 6. План развития проекта

* Добавить Hyperparameter Tuning (Optuna)
* Попробовать CatBoost
* Добавить SHAP-анализ факторов влияния
* Создать веб-сервис (FastAPI) для предсказания продаж

---

## 🙌 7. Автор

Проект подготовлен в рамках обучения и практики анализа данных и машинного обучения.
Vladimir Soloviev. Предсказание объемов продаж компьютерных игр. https://kaggle.com/competitions/findata-videogames, 2018. Kaggle.

---

